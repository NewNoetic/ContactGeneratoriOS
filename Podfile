# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ContactGenerator' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ContactGenerator
  pod "Alamofire"
  pod "PromiseKit"

  target 'ContactGeneratorTests' do
    inherit! :search_paths
    # Pods for testing
    pod "PromiseKit"
  end

end

target 'ContactGeneratoriOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ContactGeneratoriOS
  pod "PromiseKit"

end
